- use OS handles for child process debug on Win32
- test new library rules on win32
- allow user to override package detection (esp. for cross-compiling)
- documentation updates
- finish adding tests and making portable
- add abstractions:
  - sockets
- portable printf implementation, from newlib/gnulib/glibc/elsewhere?
- missing autoconf/SCons tests:
  - GLUT
  - threading API
- unit tests:
  - I/O layer
  - socket API?
- audit use of malloc/bugle_malloc/free/bugle_free for matching up
- add platform layer for glib?
- add platform layer for gnulib?
- fix build rules for chunked HTML to copy and package all the .html files
- update MinGW instructions:
  GTK+: use 2.16 bundle from http://www.gtk.org/download-windows.html (NOT >= 2.18)
  install into /mingw
  gtkglext: use source package, first fix up with
    sed -i -e 's/pangox/pangowin32/g' configure
  configure with --prefix=/mingw
    don't build in a subdir
